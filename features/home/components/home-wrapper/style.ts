import { StyleSheet } from "react-native";
import { spacing } from "themes";

export const styles = StyleSheet.create({
    contentContainer: {
        padding: spacing.xl,
        gap: spacing.md
    },
    columnWrapper: {
        gap: spacing.md
    }
})